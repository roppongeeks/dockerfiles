# Build

``` shell
> sudo docker build -t roppongeeks/substrate:dev . --build-arg PROFILE=release # Build release
> sudo docker build -t roppongeeks/substrate:dev . --build-arg PROFILE=debug   # Build debug 
```
